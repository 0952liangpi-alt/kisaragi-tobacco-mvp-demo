# Mobile P0 contract

At <=520px the existing catalog stylesheet must keep a two-column grid, compact card body, readable price/code, and a product image region above metadata. This P0 does not redesign the site; it only verifies the canonical catalog cards remain usable on mobile. Visual public-page acceptance remains required after merge.