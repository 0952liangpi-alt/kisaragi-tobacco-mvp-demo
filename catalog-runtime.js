globalThis.KISARAGI_CATALOG_RUNTIME=Object.freeze({products(){return globalThis.KISARAGI_CANONICAL_CATALOG||[]},brands(){return [...new Set(this.products().map(x=>x.brand).filter(Boolean))]},byBrand(brand){return this.products().filter(x=>x.brand===brand)},bySku(id){return this.products().find(x=>x.id===id||x.sku===id||x.product_code===id)||null},missingImages(){return this.products().filter(x=>!x.image)},conflicts(){return this.products().filter(x=>String(x.status).includes('CONFLICT'))},audit(){return globalThis.KISARAGI_CATALOG_AUDIT||null}});