(()=>{const boot=()=>{const data=globalThis.KISARAGI_JAPAN_SKUS;if(!Array.isArray(data)||!data.length)return;const anchor=document.getElementById('archive');if(!anchor||document.getElementById('jp-sku-catalog'))return;const brands=['ALL',...new Set(data.map(x=>x.brand))];const section=document.createElement('section');section.id='jp-sku-catalog';section.className='jp-sku-section';section.innerHTML=`<div class="jp-sku-wrap"><div class="jp-sku-head"><div><span class="jp-sku-kicker">JAPAN CIGARETTE REFERENCE / 57 SKU</span><h2>日本煙を、<br>ブランドから単品まで。</h2></div><p>「日本煙 → ブランド → 単品」の階層で整理。価格・商品コード・包装数・タール/ニコチンなど、公開確認できた事実フィールドをKISARAGI形式に正規化しています。</p></div><div class="jp-sku-summary"><span><b>${data.length}</b> SKU</span><span><b>${new Set(data.map(x=>x.brand)).size}</b> Brands</span><span>Source checked: 2026-09-04</span><span>画像: KISARAGI所有/許諾素材のみ</span></div><div class="jp-sku-brands"></div><div class="jp-sku-grid"></div><p class="jp-sku-note"><strong>Source policy:</strong> 商品名・価格・商品コード等の事実データを参照。参照元サイトは画像の無断使用・転用を禁止しているため、画像自体は複製せず、KISARAGI側で権利確認済み画像・実拍画像を別途紐付けます。</p></div>`;anchor.insertAdjacentElement('afterend',section);const tabs=section.querySelector('.jp-sku-brands');const grid=section.querySelector('.jp-sku-grid');const yen=n=>n==null?'—':`¥${Number(n).toLocaleString('ja-JP')}`;const draw=(brand='ALL')=>{const items=brand==='ALL'?data:data.filter(x=>x.brand===brand);grid.innerHTML=items.map(x=>`<article class="jp-sku-card"><span class="jp-sku-brand">${x.brand}</span><h3>${x.name}</h3><div class="jp-sku-meta"><span>表示価格<b>${yen(x.price)}</b></span><span>参照価格<b>${yen(x.shopPrice)}</b></span><span>商品コード<b>${x.code||'—'}</b></span><span>System<b>${x.systemCode||'—'}</b></span>${x.packCount?`<span>包装<b>${x.packCount}本</b></span>`:''}${x.tar!=null?`<span>タール<b>${x.tar}mg</b></span>`:''}${x.nicotine!=null?`<span>ニコチン<b>${x.nicotine}mg</b></span>`:''}</div><div class="jp-sku-source"><span>${x.soldOut?'SOLD OUT / ':''}${x.imageStatus==='KISARAGI_IMAGE_REQUIRED'?'IMAGE PENDING':''}</span><a href="${x.sourceUrl}" target="_blank" rel="noopener">SOURCE ↗</a></div></article>`).join('')};brands.forEach((b,i)=>{const btn=document.createElement('button');btn.type='button';btn.textContent=b==='ALL'?'すべて':b;btn.className=i===0?'active':'';btn.onclick=()=>{tabs.querySelectorAll('button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');draw(b)};tabs.appendChild(btn)});draw()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot()})();
