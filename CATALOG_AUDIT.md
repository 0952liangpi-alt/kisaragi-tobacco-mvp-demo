# KISARAGI Canonical Catalog Audit

## Existing-system classification

- **ALREADY_EXISTS** — Current site, `catalog-data.js`, World Tobacco 57-SKU Japan reference import, responsive Japan catalog UI, existing `assets/catalog/` image area, fixed GitHub Pages deployment.
- **PARTIAL** — The single canonical catalog now contains 57 reference SKUs plus 26 additional user-uploaded products. Thirty-one products have images; uploaded items without a visible exact model name remain `IDENTITY_PENDING`.
- **REAL_GAP** — Recover the remaining user-approved originals or obtain approved per-SKU images. Missing or ambiguous images remain fail-closed instead of being guessed.
- **CONFLICT** — Three Natural American Spirit Organic Mint (14) user images cannot be uniquely assigned to reference IDs `wt-1525`, `wt-1524`, `wt-1523` from visible screenshot data alone. They remain `CONFLICT_REVIEW`; no SKU is guessed.

## Current runtime truth

- `TOTAL_REFERENCE_SKU`: 57
- `TOTAL_LOCAL_SKU`: 83
- `TOTAL_ASSETS`: 32
- `TOTAL_UPLOAD_ASSETS`: 31
- `TOTAL_UPLOAD_PRODUCTS`: 30
- `IMAGE_BOUND`: 31
- `MISSING_IMAGE`: 52
- `COMPLETE_SKU`: 5
- `CONFLICTS`: 3
- `COVERAGE_PERCENT`: 6.0

These values are generated by `catalog-core.js`. The latest upload contributed 29 distinct files representing 28 product subjects after exact-hash deduplication; one product keeps two candidate images. Two exact-label Mevius images attach to existing reference SKUs. The rejected Peace wood photo and Mevius pair photo are deleted and cannot render.

## Image closeout reports

- Missing-image manifest: `reports/MISSING_IMAGE_MANIFEST.json`
- Existing-asset recovery: `reports/UNBOUND_ASSET_REPORT.json`
- Rebuild command: `node scripts/catalog-image-audit.mjs --uploads-root <upload-directory> --write`

The missing-image manifest is generated from `KISARAGI_CANONICAL_CATALOG` and `KISARAGI_ASSET_REGISTRY`, then asserted against runtime `MISSING_IMAGE`. It is not a separately maintained product list.

The 2026-09-05 recovery pass scanned 47 uploaded files: 40 are exact hashes of registered assets and seven are rejected screenshots, rejected historical product photos, or non-product screen photos. No new fail-closed SKU match was found. The remaining 52 reference SKUs are a `REAL_ASSET_GAP`; the three Natural American Spirit records remain `CONFLICT_REVIEW`.

All 32 registered files exist, decode with non-zero dimensions, and have unique hashes. Fifteen screenshot-derived assets with dark side mattes use registry-driven display cropping. Source files and image-embedded prices remain unchanged, and original-file links still open the unmodified asset.

## Phase 1 contract

Japan cigarette reference catalog is normalized into one canonical runtime catalog. User-provided imagery has priority over local verified imagery and external sources. Image-embedded prices are preserved; `price_jpy` remains a separate maintained field. Price disagreements must become `PRICE_CONFLICT`, never silent overwrite.

Pipeline:

`SOURCE → NORMALIZE → SKU MATCH → DEDUPLICATE → CONFLICT CHECK → CANONICAL CATALOG → ASSET REGISTRY → WEBSITE → VALIDATION → DEPLOYMENT`

## Phase 2 readiness

The same schema is reserved for `JAPANESE_CIGARETTES`, `IMPORTED_CIGARETTES`, `RYO`, `CIGARS`, and `PIPE_TOBACCO`. Non-applicable fields stay null/UNKNOWN; no separate database per category is permitted.

## Registries

- Canonical catalog: `catalog-core.js` / `globalThis.KISARAGI_CANONICAL_CATALOG`
- Source registry: `globalThis.KISARAGI_SOURCE_REGISTRY`
- Asset registry: `globalThis.KISARAGI_ASSET_REGISTRY`
- Per-SKU product assets: `assets/catalog/products/`
- Runtime audit: `globalThis.KISARAGI_CATALOG_AUDIT`
- Missing-image manifest: `globalThis.KISARAGI_MISSING_IMAGE_MANIFEST`
- Reference adapter: `world-tobacco-japan.js`

Final counts are generated from the canonical runtime catalog and displayed on the website catalog summary.
